export type ActorInput = {
    id: string;
};

export type USChessMemberInfo = {
    playerName: string;
    playerId: string;
    playerState: string;
    regularRating: string;
    quickRating: string;
    blitzRating: string;
    expirationDt: string;
};
